# Streams Module (Problems 79–90)

## Mnemonic
**“Flat Flat Flat Skills Reduce Prefix Sum Peek Parallel Optional.”**

- Flat → Flatten nested list (79)
- Flat → FlatMap sentences (80)
- Flat → FlatMap list of lists (81)
- Skills → FlatMap employees skills (82)
- Reduce → Concatenate strings (83)
- Prefix → Longest common prefix (84)
- Sum → Reduce sum/max (85)
- Peek → Debugging pipeline (86)
- Parallel → Parallel streams (87)
- Optional → Avoid NPE, ifPresent, stream conversion (88–90)

## Key Focus
- flatMap for flattening nested structures
- reduce for aggregation (concat, prefix, sum, max)
- peek for debugging stream pipelines
- parallelStream for large data sets
- Optional to handle nulls safely

## Mnemonic
“Flat Flat Flat Skills Reduce Prefix Sum Peek Parallel Optional.”

Flat → Flatten nested list (79)  
Question: Flatten nested list into distinct sorted elements.

Flat → FlatMap sentences (80)  
Question: Extract unique words from sentences using flatMap.

Flat → FlatMap list of lists (81)  
Question: Flatten list of lists into single list.

Skills → FlatMap employees skills (82)  
Question: Extract unique skills from employees using flatMap.

Reduce → Concatenate strings (83)  
Question: Concatenate strings using reduce.

Prefix → Longest common prefix (84)  
Question: Find longest common prefix using reduce.

Sum → Reduce sum/max (85)  
Question: Find sum and max using reduce.

Peek → Debugging pipeline (86)  
Question: Use peek() to debug stream pipeline.

Parallel → Parallel streams (87)  
Question: Use parallel streams for large data sets.

Optional → Avoid NPE (88)  
Question: Use Optional to avoid NullPointerException.

Optional → ifPresent/ifPresentOrElse (89)  
Question: Use Optional.ifPresentOrElse for safe handling.

Optional → Stream conversion (90)  
Question: Convert Optional to Stream.

Functions Module (Problems 91–94)
Mnemonic
“Predicate Function Consumer Supplier.”

Predicate → Chain multiple Predicates (91)  
Question: Chain multiple predicates with and/or.

Function → andThen vs compose (92)  
Question: Demonstrate difference between andThen and compose.

Consumer → andThen chaining (93)  
Question: Chain multiple consumers with andThen.

Supplier → Lazy object creation (94)  
Question: Use Supplier for lazy instantiation.