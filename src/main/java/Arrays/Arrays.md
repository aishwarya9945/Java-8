# Arrays Module (Problems 46–65)

## Mnemonic
**“Remove Sort First Skip Limit Rotate Partition Filter Clean Common Diff Unique Square Seq Rotate Zero Color Parity Remove.”**

- Remove → Remove duplicates (46)
- Sort → Asc/Desc (47)
- First → First element (48)
- Skip → Skip N (49)
- Limit → Limit N (50)
- Rotate → Rotate list (51)
- Partition → Even/Odd (52)
- Filter → Strings by length (53)
- Clean → Remove nulls/empty (54–55)
- Common → Common elements (56)
- Diff → Difference (57)
- Unique → Appearing once (58)
- Square → Squares (59)
- Seq → Longest consecutive sequence (60)
- Rotate → Array rotation (61)
- Zero → Move zeroes (62)
- Color → Sort colors (63)
- Parity → Sort by parity (64)
- Remove → Remove element (65)

## Key Focus
- Stream operations on lists (distinct, sorted, skip, limit, partitioningBy)
- Cleaning data (nulls, blanks)
- Set operations (common, difference, unique)
- Array algorithms (rotation, move zeroes, Dutch flag, parity sort)
- In-place modifications vs creating new arrays

## “Remove Sort First Skip Limit Rotate Partition Filter Clean Common Diff Unique Square Seq Rotate Zero Color Parity Remove.”

Remove → Remove duplicates (46)  
Question: Remove duplicates from an array using streams.

Sort → Sort ascending/descending (47)  
Question: Sort an array ascending/descending using streams.

First → Find first element (48)  
Question: Find the first element of a list using streams.

Skip → Skip N elements (49)  
Question: Skip N elements in a list using streams.

Limit → Limit first N elements (50)  
Question: Limit to first N elements using streams.

Rotate → Rotate list by N positions (51)  
Question: Rotate a list by N positions.

Partition → Even/odd (52)  
Question: Partition list into even and odd numbers.

Filter → Strings by length (53)  
Question: Filter strings longer than given length.

Clean → Remove nulls (54)  
Question: Remove null values from a list.

Clean → Remove empty/blank strings (55)  
Question: Remove empty or blank strings.

Common → Common elements (56)  
Question: Find common elements between two lists.

Diff → Difference (57)  
Question: Find difference between two lists.

Unique → Appearing once (58)  
Question: Find elements appearing exactly once.

Square → Squares (59)  
Question: Convert list to squares of numbers.

Seq → Longest consecutive sequence (60)  
Question: Find longest consecutive sequence in array.

Rotate → Array rotation (61)  
Question: Rotate array using reverse trick.

Zero → Move zeroes (62)  
Question: Move zeroes to end of array.

Color → Sort colors (63)  
Question: Sort colors using Dutch flag algorithm.

Parity → Sort by parity (64)  
Question: Sort array by parity.

Remove → Remove element (65)  
Question: Remove given element from array.