# Scenarios Module (Problems 107–121)

## Mnemonic
**“Merge Top Third Digit Match Sorted Rotate Long Longer Join CSV Distinct Salary Union Pairs.”**

- Merge → Merge arrays sorted (107)
- Top → Top 3 elements (108)
- Third → 3rd highest (109)
- Digit → Numbers starting with 1 (110)
- Match → allMatch/anyMatch/noneMatch (111)
- Sorted → Check list sorted (112)
- Rotate → Rotate list (113)
- Long → Longest string (114)
- Longer → Strings longer than n (115)
- Join → Comma separated string (116)
- CSV → Split CSV sorted (117)
- Distinct → Distinct preserving order (118)
- Salary → Salaries grouped sum (119)
- Union → Union & intersection (120)
- Pairs → All pairs summing to target (121)

## Key Focus
- Real-world interview scenarios
- Combining streams, collectors, arrays, sets
- Practical tasks: merging, rotating, filtering, grouping
- Problem-solving patterns for coding interviews

## Mnemonic
“Merge Top Third Digit Match Sorted Rotate Long Longer Join CSV Distinct Salary Union Pairs.”

Merge → Merge arrays sorted (107)  
Question: Merge two arrays into one sorted array using streams.

Top → Top 3 elements (108)  
Question: Find the top 3 largest elements in a list.

Third → 3rd highest element (109)  
Question: Find the 3rd highest element in a list using streams.

Digit → Numbers starting with 1 (110)  
Question: Find numbers starting with digit “1” in a list.

Match → allMatch/anyMatch/noneMatch (111)  
Question: Check if all, any, or none of the elements match a condition.

Sorted → Check list sorted (112)  
Question: Verify if a list is sorted in ascending order.

Rotate → Rotate list (113)  
Question: Rotate a list by N positions using streams.

Long → Longest string (114)  
Question: Find the longest string in a list.

Longer → Strings longer than N (115)  
Question: Find all strings longer than a given length.

Join → Comma-separated string (116)  
Question: Convert a list into a comma-separated string.

CSV → Split CSV sorted (117)  
Question: Split a CSV string and sort the elements.

Distinct → Distinct preserving order (118)  
Question: Find distinct elements in a list while preserving order.

Salary → Salaries grouped sum (119)  
Question: Group employees by department and calculate total salary.

Union → Union & intersection (120)  
Question: Find union and intersection of two sets.

Pairs → Pairs summing to target (121)  
Question: Find all pairs of numbers in a list that sum to a target value.