# Maps Module (Problems 66–78)

## Mnemonic
**“Word Char Most Top Freq Dup Filter Sort Compute Merge ToMap Mapping Collect.”**

- Word → Word frequency (66)
- Char → Character frequency (67)
- Most → Most frequent element (68)
- Top → Top K frequent (69)
- Freq → Word frequency again (70)
- Dup → Duplicate words (71)
- Filter → Filter map by value (72)
- Sort → Sort map by value descending (73)
- Compute → computeIfAbsent list map (74)
- Merge → merge for frequencies (75)
- ToMap → toMap with duplicate handling (76)
- Mapping → mapping inside groupingBy (77)
- Collect → collectingAndThen unmodifiable list (78)

## Key Focus
- Frequency counting (words, chars, duplicates)
- Filtering and sorting maps
- Advanced map operations (merge, computeIfAbsent)
- Collectors tricks (toMap, mapping, collectingAndThen)

## Mnemonic
“Word Char Most Top Freq Dup Filter Sort Compute Merge ToMap Mapping Collect.”

Word → Count frequency of words (66)  
Question: Count frequency of words in a sentence.

Char → Count frequency of characters (67)  
Question: Count frequency of characters in a string.

Most → Most frequent element (68)  
Question: Find most frequent element in list.

Top → Top K frequent elements (69)  
Question: Find top K frequent elements in list.

Freq → Word frequency again (70)  
Question: Count frequency of words in string.

Dup → Duplicate words (71)  
Question: Find duplicate words in sentence.

Filter → Filter map by value (72)  
Question: Filter map entries by value condition.

Sort → Sort map by value descending (73)  
Question: Sort map entries by value descending.

Compute → computeIfAbsent (74)  
Question: Use computeIfAbsent to build list-valued map.

Merge → merge for frequencies (75)  
Question: Use merge() to count word frequencies.

ToMap → toMap with duplicate handling (76)  
Question: Use toMap() with merge function for duplicates.

Mapping → mapping inside groupingBy (77)  
Question: Use mapping() inside groupingBy.

Collect → collectingAndThen (78)  
Question: Use collectingAndThen to create unmodifiable list.